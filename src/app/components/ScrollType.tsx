// The scroll-typing effect was removed. This module is intentionally empty;
// body text now renders directly. Kept as a stub because the file could not be
// deleted from this environment — it is imported by nothing.
export {};
